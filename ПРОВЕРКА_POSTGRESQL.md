# 🔍 Проверка PostgreSQL на Railway

## Шаг 1: Проверьте переменные на Railway

1. Откройте https://railway.app
2. Выберите ваш проект
3. Перейдите в **Settings** → **Variables**
4. Найдите переменную `DATABASE_URL`

### Если `DATABASE_URL` есть:
✅ PostgreSQL подключен!

Переменная должна выглядеть примерно так:
```
DATABASE_URL=postgresql://postgres:password@hostname:5432/railway
```

### Если `DATABASE_URL` нет:
❌ PostgreSQL не подключен к вашему приложению!

**Решение:**
1. Убедитесь что PostgreSQL база создана (должна быть в списке сервисов)
2. Нажмите на PostgreSQL сервис
3. Перейдите в **Variables**
4. Скопируйте значение `DATABASE_URL`
5. Вернитесь к вашему приложению
6. Перейдите в **Settings** → **Variables**
7. Добавьте новую переменную:
   - Name: `DATABASE_URL`
   - Value: (вставьте скопированное значение)

---

## Шаг 2: Проверьте что код использует PostgreSQL

Откройте логи на Railway:
1. Перейдите в **Deployments**
2. Выберите последний деплой
3. Нажмите **View Logs**

### Ищите эту строку:

✅ **Если видите:**
```
✅ Используется PostgreSQL
🗄️  База данных: PostgreSQL
```
Значит PostgreSQL работает!

❌ **Если видите:**
```
📁 База данных: JSON файлы
⚠️  DATABASE_URL не найден, используются JSON файлы
```
Значит PostgreSQL НЕ подключен!

---

## Шаг 3: Если PostgreSQL не работает

### Вариант А: DATABASE_URL не установлен
Следуйте инструкциям из Шага 1 выше.

### Вариант Б: Код не поддерживает PostgreSQL
Текущий `server.js` уже поддерживает PostgreSQL!

Проверьте в начале файла `server.js`:
```javascript
const USE_POSTGRES = !!process.env.DATABASE_URL;
```

Если этой строки нет - значит используется старая версия кода.

---

## Шаг 4: Тест

После настройки:
1. Откройте админку
2. Добавьте несколько товаров
3. Перезапустите приложение на Railway:
   - Settings → Restart
4. Откройте админку снова
5. Товары должны остаться! ✅

---

## 💡 Быстрая диагностика

Выполните эти проверки по порядку:

1. ☐ PostgreSQL база создана на Railway?
2. ☐ Переменная `DATABASE_URL` установлена в приложении?
3. ☐ В логах написано "Используется PostgreSQL"?
4. ☐ Товары сохраняются после перезапуска?

Если на все вопросы ✅ - все работает!
Если хоть один ❌ - напишите какой, я помогу!
