# 🚀 Деплой на Railway через GitHub

## Шаг 1: Подготовка GitHub

### 1.1 Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Название: `danisa-shop-bot` (или как вам нравится)
3. Описание: "Telegram Mini App Shop Bot"
4. Выберите "Public" или "Private"
5. **НЕ** добавляйте .gitignore (уже есть)
6. Нажмите "Create repository"

### 1.2 Залейте проект на GitHub

Откройте PowerShell в папке `c:\shop` и выполните:

```powershell
# Инициализируйте git (если еще не инициализировано)
git init

# Добавьте все файлы
git add .

# Коммит
git commit -m "Initial commit: DANISA SHOP Bot"

# Добавьте remote (замените ВАШ_ЛОГИН на ваш GitHub логин)
git remote add origin https://github.com/ВАШ_ЛОГИН/danisa-shop-bot.git

# Отправьте на GitHub
git branch -M main
git push -u origin main
```

---

## Шаг 2: Деплой на Railway

### 2.1 Создайте аккаунт на Railway

1. Перейдите на https://railway.app
2. Нажмите "Login with GitHub"
3. Авторизуйтесь через GitHub

### 2.2 Создайте новый проект

1. На главной странице Railway нажмите "+ New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите репозиторий `danisa-shop-bot`
4. Нажмите "Deploy Now"

### 2.3 Добавьте переменные окружения

1. В Railway перейдите в проект → Variables
2. Добавьте переменные из `.env`:
   ```
   TELEGRAM_BOT_TOKEN=8232572053:AAF5zxkQ-EpEAAkQPw_FUYQnO45PdfkrwB8
   ADMIN_PASSWORD=admin123
   ADMIN_CHAT_ID=7855745005
   ADMIN_TELEGRAM_ID=7855745005
   PORT=3000
   ```
3. **SERVER_URL** - добавьте позже, когда Railway даст вам URL

### 2.4 Получите публичный URL

1. В Railway нажмите на сервис (Node.js)
2. В разделе "Networking" скопируйте URL (например: `https://abc123.railway.app`)
3. Добавьте переменную:
   ```
   SERVER_URL=https://abc123.railway.app
   ```

### 2.5 Перезагрузите приложение

1. Нажмите кнопку "Redeploy" в Railway
2. Дождитесь завершения деплоя (должна быть зеленая галочка)

---

## Шаг 3: Настройка Telegram Webhook

После того как приложение развернуто на Railway:

1. Откройте браузер и выполните запрос (замените URL на ваш):
   ```
   https://api.telegram.org/bot8232572053:AAF5zxkQ-EpEAAkQPw_FUYQnO45PdfkrwB8/setWebhook?url=https://abc123.railway.app/telegram
   ```

2. Или запустите в PowerShell:
   ```powershell
   curl "https://api.telegram.org/bot8232572053:AAF5zxkQ-EpEAAkQPw_FUYQnO45PdfkrwB8/setWebhook?url=https://abc123.railway.app/telegram"
   ```

3. Должен получить ответ: `{"ok":true,"result":true,...}`

---

## Готово! ✅

Теперь:
- Бот работает на публичном сервере
- Web App открывается прямо в Telegram
- Все заказы сохраняются в памяти приложения (пока не перезагрузится)

**Для постоянного хранения данных** (опционально):
- Используйте Railway PostgreSQL или MongoDB
- Или прикрепите Volume для сохранения файлов

---

## Команды для тестирования

```powershell
# Проверить статус бота
curl "https://api.telegram.org/bot8232572053:AAF5zxkQ-EpEAAkQPw_FUYQnO45PdfkrwB8/getMe"

# Проверить webhook информацию
curl "https://api.telegram.org/bot8232572053:AAF5zxkQ-EpEAAkQPw_FUYQnO45PdfkrwB8/getWebhookInfo"
```

---

## Помощь

Если что-то не работает:
- Проверьте логи в Railway (Logs tab)
- Убедитесь, что все переменные окружения установлены
- Перезагрузите приложение (Redeploy)

Вопросы? Напиши! 💬
