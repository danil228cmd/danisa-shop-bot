# DANISA SHOP - БЕЗ NPM (ГОТОВО К РАБОТЕ!)

## ✅ Что получилось:

✨ **Полнофункциональный Telegram Mini App магазин одежды**

- 🎁 Встроенные модули Node.js — БЕЗ зависимостей!
- 📦 JSON хранилище вместо БД (папка `./data/`)
- 🖼️ Загрузка фото на сервер (папка `./uploads/`)
- 🛍️ Красивый интерфейс магазина (как на картинке)
- ⚙️ Админ-панель для управления товарами
- 🔔 Уведомления в Telegram при новых заказах
- 🛒 Корзина сохраняется между заходами
- 🔒 Защита паролем

---

## 🚀 ЗАПУСК ЗА 10 СЕКУНД

Откройте **PowerShell** в папке `c:\shop`:

```powershell
node server.js
```

**Готово!** Сервер работает на:
- **Админ панель:** http://localhost:3000/admin/
- **Магазин:** http://localhost:3000/miniapp/

Пароль: `admin123`

---

## 📋 Файлы проекта

```
c:\shop/
├── server.js                    # Всё! Один файл со всем API
├── package.json                 # Конфигурация (без зависимостей)
├── .env                         # Ваши данные (Telegram token и т.д.)
├── public/
│   ├── miniapp/index.html       # Магазин для покупателей
│   └── admin/index.html         # Админ-панель
├── data/                        # Автоматически создаётся
│   ├── categories.json          # Категории
│   ├── subcategories.json       # Подкатегории
│   ├── products.json            # Товары
│   ├── orders.json              # Заказы
│   └── carts.json               # Корзины пользователей
└── uploads/                     # Загруженные фото

```

---

## 🔧 КОНФИГУРАЦИЯ

Отредактируйте файл `.env`:

```powershell
notepad .env
```

**Обязательно:**
```
ADMIN_PASSWORD=admin123        # Измените пароль!
PORT=3000
```

**Опционально (для Telegram уведомлений):**
```
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHI...
ADMIN_CHAT_ID=987654321
SERVER_URL=http://localhost:3000
```

---

## 📖 БЫСТРЫЙ СТАРТ

### 1. Добавить категорию

**В браузере:**
1. Откройте http://localhost:3000/admin/
2. Введите пароль: `admin123`
3. На вкладке "Категории" напишите: "Куртки"
4. Нажмите кнопку

### 2. Добавить подкатегорию

1. Перейдите на вкладку "Подкатегории"
2. Выберите "Куртки"
3. Напишите "Stone Island"
4. Нажмите кнопку

### 3. Добавить товар

1. Перейдите на вкладку "Товары"
2. Выберите подкатегорию "Stone Island"
3. Заполните:
   - Название: `Puffer Jacket`
   - Описание: `Высокого качества пуховая куртка`
   - Цена: `29990`
4. Нажмите кнопку
5. Нажмите кнопку **"Фото"** рядом с товаром
6. Загрузите картинку

### 4. Протестировать магазин

1. Откройте http://localhost:3000/miniapp/
2. Нажмите на товар
3. Добавьте в корзину
4. Откройте корзину (🛒 вверху)
5. Оформите заказ (вводите имя и контакт)
6. ✅ Заказ создан!

### 5. Посмотреть заказы

В админ-панели нажмите вкладку "Заказы" — вы увидите все заказы с деталями.

---

## 🌐 ТЕСТИРОВАНИЕ С ДРУЗЬЯМИ

### Локально (на одной машине):

1. Запустите сервер
2. Дайте друзьям ссылку: `http://ВАШ_IP:3000/miniapp/`
   - Узнать IP: `ipconfig` (IPv4 Address)
   - Например: `http://192.168.1.100:3000/miniapp/`

### Через интернет (ngrok):

1. **Скачайте ngrok:** https://ngrok.com/download
2. **Запустите:**
```powershell
C:\ngrok\ngrok http 3000
```

3. **Скопируйте URL** (вроде `https://abc123.ngrok.io`)
4. **Обновите `.env`:**
```
SERVER_URL=https://abc123.ngrok.io
```

5. **Дайте друзьям:** `https://abc123.ngrok.io/miniapp/`

---

## 📞 TELEGRAM ИНТЕГРАЦИЯ

### Получить Bot Token:

1. Откройте Telegram → найдите **@BotFather**
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. **Скопируйте токен**

### Получить Chat ID:

1. Отправьте сообщение своему боту в Telegram
2. Откройте в браузере:
```
https://api.telegram.org/bot<ВАШЕ_ТОКЕН>/getUpdates
```
(замените `<ВАШЕ_ТОКЕН>`)

3. Найдите `"id":123456789` — это ваш Chat ID

### Настроить уведомления:

Отредактируйте `.env`:
```
TELEGRAM_BOT_TOKEN=123456789:ABCdef...
ADMIN_CHAT_ID=987654321
```

Перезагрузите сервер (Ctrl+C, потом `node server.js`)

**Теперь при каждом заказе вам придёт сообщение в Telegram!** 📬

---

## 🛠️ УПРАВЛЕНИЕ ДАННЫМИ

### Где хранятся данные?

Все данные в папке `./data/` в JSON файлах:
- Категории → `categories.json`
- Подкатегории → `subcategories.json`
- Товары → `products.json`
- Заказы → `orders.json`
- Корзины → `carts.json`

**Вы можете:**
- ✅ Отредактировать вручную в текстовом редакторе
- ✅ Сделать бэкап (скопировать папку `data/`)
- ✅ Удалить и начать заново (просто удалите файлы, они пересоздадутся)

### Где хранятся фото?

Папка `./uploads/` — все загруженные изображения.

---

## 🎨 КАСТОМИЗАЦИЯ

### Изменить пароль администратора:

В `.env`:
```
ADMIN_PASSWORD=your_secret_password
```

### Изменить название:

В файлах `public/miniapp/index.html` и `public/admin/index.html` найдите `"DANISA SHOP"` и замените.

### Изменить цвета:

В `public/miniapp/index.html` и `public/admin/index.html` найдите:
```css
:root {
  --primary: #000;      /* Чёрный */
  --accent: #ff6b6b;    /* Красный */
  --secondary: #f5f5f5; /* Светлый фон */
}
```

Поменяйте на свои!

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "Port 3000 is already in use"

**Решение:** Измените порт в `.env`:
```
PORT=3001
```

Или килльте процесс:
```powershell
Get-Process -Name node | Stop-Process -Force
```

### Сервер не запускается

Проверьте:
```powershell
node --version  # Должно быть 14+
```

### Админ-панель не загружается

- Очистите кэш браузера (Ctrl+Shift+Del)
- Попробуйте в приватном окне

### Фото не загружаются

- Проверьте, что папка `uploads/` существует
- Убедитесь, что права доступа ОК

### Заказы не приходят в Telegram

- Проверьте `TELEGRAM_BOT_TOKEN` (в `.env`)
- Проверьте `ADMIN_CHAT_ID` (только цифры!)
- Отправьте сообщение боту сначала
- Перезагрузите сервер

---

## 📊 API ENDPOINTS

Если вам нужен API для своего приложения:

### Получить категории:
```
GET /api/categories
```

### Получить товары:
```
GET /api/products
GET /api/products?subcategoryId=1
GET /api/products/1
```

### Получить корзину:
```
GET /api/cart/123456789
```

### Создать заказ:
```
POST /api/orders
```

Подробнее в файле `API_EXAMPLES.md`

---

## 🚀 ДЛЯ ПРОДАКШЕНА

### Развернуть на облако:

**Рекомендую:**
1. **Replit** — быстро, бесплатно, с веб-интерфейсом
2. **Railway** — просто, бесплатный тир хороший
3. **Heroku** — классика (платный)
4. **DigitalOcean** — мощнее

### На любой платформе:

1. Загрузьте все файлы
2. Запустите: `node server.js`
3. Установите `TELEGRAM_BOT_TOKEN` в переменные окружения
4. Обновите `SERVER_URL` на реальный домен

---

## 📞 КОНТАКТЫ ПОКУПАТЕЛЯ

Во время оформления заказа пользователь вводит:
- **Имя/Nickname** — как его зовут
- **Телефон или способ связи** — что угодно (номер, @username, Telegram ID)

Вы увидите это в админ-панели и в Telegram уведомлении.

---

## ✅ ГОТОВО!

Всё работает **БЕЗ npm install**, **БЕЗ зависимостей**, **только встроенные модули Node.js**.

**Что дальше:**
1. ✅ Запустить сервер
2. ✅ Добавить товары через админ-панель
3. ✅ Отправить другу ссылку на магазин
4. ✅ Получить заказ
5. ✅ Радоваться! 🎉

---

**Вопросы? Смотрите:**
- `QUICKSTART.md` — для новичков
- `API_EXAMPLES.md` — примеры запросов
- `README.md` — полная документация

**Успехов! 🚀**
