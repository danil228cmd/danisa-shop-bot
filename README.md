<<<<<<< HEAD
# DANISA SHOP - Telegram Mini App Bot

Полнофункциональный магазин одежды с Telegram Web App интеграцией.

## 🎯 Возможности

### Для покупателей:
- 🛍️ Каталог товаров с фильтрацией по категориям
- 🔄 Сортировка по цене, названию
- 🖼️ Карусель фотографий товаров
- 🛒 Корзина (сохраняется при переходе)
- 📦 Оформление заказа с контактными данными

### Для администратора:
- ➕ Добавление категорий и подкатегорий
- 📝 Управление товарами (добавление, редактирование, удаление)
- 🖼️ Загрузка изображений товаров
- 📊 Просмотр статистики (кол-во заказов, товаров, выручка)
- 📬 Просмотр всех заказов с деталями

### Интеграция:
- ✅ Telegram Web App (Mini App)
- 🔔 Уведомления администратору при новых заказах
- 💾 Сохранение корзины пользователя в БД
- 🔐 Защита админ-панели паролем

---

## 📦 Установка

### Требования:
- Node.js (v14+)
- npm или yarn

### Шаги установки:

1. **Перейдите в папку проекта:**
```powershell
cd c:\shop
```

2. **Установите зависимости:**
```powershell
npm install
```

3. **Создайте файл `.env`** (уже есть в папке, отредактируйте):
```powershell
# Отредактируйте .env файл
notepad .env
```

**Необходимые переменные в `.env`:**
```
TELEGRAM_BOT_TOKEN=<ВАШ_TOKEN_БОТА>
ADMIN_PASSWORD=admin123
ADMIN_CHAT_ID=<ВАШ_CHAT_ID>
SERVER_URL=http://localhost:3000
PORT=3000
```

---

## 🤖 Получение Telegram Bot Token

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (введите имя бота, username)
4. Скопируйте полученный токен в переменную `TELEGRAM_BOT_TOKEN` в `.env`

**Пример токена:** `123456789:ABCdefGHIjklmnoPQRstuvWXYZabcdefg`

---

## 🔑 Получение Chat ID

Для отправки уведомлений администратору:

1. Отправьте сообщение вашему боту в Telegram
2. Откройте в браузере: `https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates`
3. Найдите `"chat":{"id":123456789}` — это ваш Chat ID
4. Вставьте в `.env` переменную `ADMIN_CHAT_ID`

---

## 🚀 Запуск

### Локально:

```powershell
npm start
```

Сервер запустится на `http://localhost:3000`

- 🛍️ **Магазин (Mini App):** http://localhost:3000/miniapp/
- ⚙️ **Админ панель:** http://localhost:3000/admin/
  - Пароль по умолчанию: `admin123` (измените в `.env`)

---

## 🌐 Развёртывание (для тестирования с друзьями)

### Вариант 1: Использование ngrok (быстро для локального тестирования)

1. **Скачайте ngrok:** https://ngrok.com/download
2. **Запустите ngrok:**
```powershell
ngrok http 3000
```

3. **Скопируйте URL** (например: `https://abc123.ngrok.io`)
4. **Обновите `.env`:**
```
SERVER_URL=https://abc123.ngrok.io
```

5. **Перезагрузите бот** и дайте друзьям ссылку на бота!

### Вариант 2: Облачные платформы

Для production рекомендуется развёртывание на:
- **Heroku** (простой deploy)
- **Railway** (альтернатива Heroku)
- **Replit** (бесплатно, с Web интерфейсом)
- **AWS, DigitalOcean** (более надежные)

---

## 📖 Использование

### Для покупателя:
1. Откройте бота в Telegram
2. Нажмите кнопку **"🛍️ Открыть магазин"**
3. Смотрите товары, добавляйте в корзину
4. Оформите заказ с указанием контактных данных
5. Администратор получит уведомление

### Для администратора:
1. Откройте бота в Telegram
2. Нажмите кнопку **"⚙️ Админ панель"**
3. Введите пароль (`admin123` или тот, что в `.env`)
4. Управляйте категориями, товарами, просматривайте заказы

---

## 🗂️ Структура проекта

```
shop/
├── server.js           # Основной Express сервер и API
├── bot.js              # Standalone бот (опционально)
├── package.json        # Зависимости
├── .env                # Конфигурация
├── db/
│   ├── init.js         # Инициализация SQLite БД
│   └── db.js           # Хелперы для работы с БД
│   └── shop.db         # SQLite файл БД (создаётся автоматически)
├── public/
│   ├── miniapp/
│   │   └── index.html  # Магазин для пользователей
│   └── admin/
│       └── index.html  # Админ панель
└── uploads/            # Папка для загруженных изображений

```

---

## 🔧 API Endpoints

### Общественные:
- `GET /api/categories` - Получить категории
- `GET /api/subcategories/:categoryId` - Получить подкатегории
- `GET /api/products` - Получить товары
- `GET /api/products/:id` - Получить деталь товара
- `GET /api/cart/:userId` - Получить корзину пользователя
- `POST /api/cart/:userId` - Сохранить корзину
- `POST /api/orders` - Создать заказ

### Администратор (требуется пароль):
- `POST /api/categories` - Добавить категорию
- `DELETE /api/categories/:id` - Удалить категорию
- `POST /api/subcategories` - Добавить подкатегорию
- `DELETE /api/subcategories/:id` - Удалить подкатегорию
- `POST /api/products` - Добавить товар
- `PUT /api/products/:id` - Обновить товар
- `DELETE /api/products/:id` - Удалить товар
- `POST /api/products/:id/upload-image` - Загрузить изображение
- `DELETE /api/product-images/:id` - Удалить изображение
- `GET /api/orders` - Получить все заказы
- `GET /api/orders/:id` - Получить деталь заказа

---

## 📝 Примеры команд PowerShell

### Запуск сервера:
```powershell
cd c:\shop
npm install
npm start
```

### Проверка статуса сервера:
```powershell
# Отправьте GET запрос (используя PowerShell)
Invoke-WebRequest -Uri "http://localhost:3000/api/categories"
```

### Создание категории через API (пример):
```powershell
$body = @{
    name = "Куртки"
    password = "admin123"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:3000/api/categories" `
  -Method POST `
  -Headers @{"Content-Type"="application/json"} `
  -Body $body
```

---

## 🎨 Кастомизация

### Изменить пароль администратора:
Отредактируйте `.env`:
```
ADMIN_PASSWORD=your_new_password
```

### Изменить цвета и стили:
В файлах `public/miniapp/index.html` и `public/admin/index.html` найдите CSS переменные:
```css
:root {
  --primary: #000;      /* Основной цвет */
  --accent: #ff6b6b;    /* Акцент (красный) */
  --secondary: #f5f5f5; /* Фон */
}
```

### Добавить новые поля в товар:
Отредактируйте схему БД в `db/init.js` и API в `server.js`

---

## 🐛 Решение проблем

### Ошибка "EADDRINUSE: address already in use :::3000"
Порт 3000 уже занят. Измените в `.env`:
```
PORT=3001
```

### Бот не отправляет уведомления
- Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
- Проверьте `ADMIN_CHAT_ID` (должен быть число, например: `123456789`)
- Убедитесь, что сервер запущен

### Изображения не загружаются
- Проверьте папку `uploads/` (должна существовать)
- Убедитесь, что у приложения есть права на запись

### Mini App не открывается в Telegram
- Убедитесь, что `SERVER_URL` в `.env` доступен по HTTPS (ngrok автоматически даёт https)
- Проверьте консоль браузера (F12) на ошибки

---

## 📞 Поддержка

Если возникли вопросы или проблемы, проверьте:
1. Консоль сервера (могут быть ошибки)
2. Консоль браузера (F12 → Console)
3. Network вкладка (проверьте API запросы)

---

## 🚀 Готово к деплою!

Теперь вы можете:
1. ✅ Запустить сервер локально
2. ✅ Тестировать с друзьями через ngrok
3. ✅ Развернуть на облачной платформе
4. ✅ Управлять магазином через админ-панель
5. ✅ Принимать заказы через бота

**Наслаждайтесь! 🎉**
=======
# danisa-shop-bot
>>>>>>> bc27642172d12a1cfa1ee5e5a3bbe252bcb25be1
