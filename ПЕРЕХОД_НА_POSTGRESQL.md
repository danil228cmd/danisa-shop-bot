# 🚀 Переход на PostgreSQL (Railway)

## Зачем это нужно?
PostgreSQL сохранит все ваши товары и заказы даже после деплоя!

## Шаги установки

### 1️⃣ Добавить PostgreSQL на Railway

1. Откройте ваш проект на Railway
2. Нажмите **+ New**
3. Выберите **Database** → **Add PostgreSQL**
4. Railway автоматически создаст базу данных
5. Переменная `DATABASE_URL` будет добавлена автоматически

### 2️⃣ Обновить код

Замените `server.js` на новую версию:

```bash
# Переименуйте старый файл (на всякий случай)
mv server.js server-old.js

# Переименуйте новый файл
mv server-postgres.js server.js
```

Или просто удалите `server.js` и переименуйте `server-postgres.js` в `server.js`

### 3️⃣ Загрузить на GitHub

```bash
git add .
git commit -m "Добавлена поддержка PostgreSQL"
git push
```

Railway автоматически задеплоит изменения!

### 4️⃣ Проверить

1. Откройте админ панель
2. Добавьте несколько товаров
3. Сделайте еще один push в GitHub
4. Проверьте - товары должны остаться!

## ✅ Преимущества PostgreSQL

- ✅ Данные не пропадают при деплое
- ✅ Быстрее чем JSON файлы
- ✅ Бесплатно на Railway
- ✅ Профессиональное решение
- ✅ Автоматические бэкапы

## 🔄 Обратная совместимость

Новый код работает в двух режимах:

- **Если есть `DATABASE_URL`** → использует PostgreSQL
- **Если нет `DATABASE_URL`** → использует JSON файлы (как раньше)

Это значит, что код будет работать и локально (с JSON), и на Railway (с PostgreSQL)!

## 📝 Что изменилось?

- `server.js` → теперь поддерживает PostgreSQL
- `db/postgres.js` → новый модуль для работы с базой
- `package.json` → добавлена зависимость `pg`

## ❓ Проблемы?

Если что-то не работает:

1. Проверьте, что PostgreSQL добавлен в Railway
2. Проверьте логи: Railway → Deploy → View Logs
3. Убедитесь, что `DATABASE_URL` установлен: Settings → Variables

## 🎉 Готово!

Теперь ваши товары будут сохраняться навсегда!
