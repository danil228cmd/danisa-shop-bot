# 🔧 Решение проблемы пропадания данных на Railway

## Проблема
При каждом деплое на Railway все данные (товары, заказы) пропадают, потому что они хранятся в JSON файлах внутри контейнера, который пересоздается при каждом деплое.

## Решение 1: Использовать Railway Volume (Рекомендуется)

### Шаг 1: Создать Volume в Railway
1. Откройте ваш проект на Railway
2. Перейдите в раздел **Settings** → **Volumes**
3. Нажмите **+ New Volume**
4. Укажите:
   - **Mount Path**: `/app/data`
   - **Size**: 1GB (достаточно для начала)
5. Сохраните

### Шаг 2: Перезапустить приложение
После создания Volume Railway автоматически перезапустит приложение, и теперь папка `data/` будет постоянной.

### Шаг 3: Загрузить существующие данные (если есть)
Если у вас уже есть товары локально:
1. Скопируйте файлы из папки `data/` (categories.json, products.json и т.д.)
2. Используйте Railway CLI для загрузки:
```bash
railway login
railway link
railway run bash
# В открывшейся консоли:
cat > /app/data/categories.json
# Вставьте содержимое файла и нажмите Ctrl+D
```

## Решение 2: Использовать базу данных PostgreSQL

Если хотите более надежное решение, можно переключиться на PostgreSQL:

1. В Railway добавьте **PostgreSQL** плагин
2. Railway автоматически создаст переменную `DATABASE_URL`
3. Нужно будет переписать код для работы с PostgreSQL вместо JSON файлов

## Решение 3: Использовать переменные окружения для начальных данных

Можно хранить начальные категории в `.env` файле, но это подходит только для небольшого количества данных.

## Проверка

После настройки Volume:
1. Добавьте несколько товаров через админ панель
2. Сделайте новый деплой (push в GitHub)
3. Проверьте, что товары остались на месте

## Важно!

⚠️ **Не забудьте добавить папку `uploads/` в Volume**, если храните изображения локально:
- Создайте еще один Volume с Mount Path: `/app/uploads`

Или используйте облачное хранилище для изображений (Cloudinary, AWS S3).
